# Standalone STTM backend — copy to .env (optional for local dev)
#
# LLM keys are set in the UI: Settings → LLM Provider & API Key
# (saved here automatically when you save in Settings)
#
# Optional overrides:

# DATAMAP_CORS_ORIGINS=https://your-frontend-host
